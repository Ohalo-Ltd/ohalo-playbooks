# DXR
DXR_APP_URL=https://demo.dataxray.io
DXR_PAT_TOKEN=__REDACTED__

# Box (choose one auth strategy)
# Quick testing only:
# Obtain a short-lived developer token from Box Developer Console
BOX_DEVELOPER_TOKEN=

# Production (recommended): JWT App
# Path to the Box JWT app config JSON (never commit real secrets)
# BOX_CONFIG_JSON_PATH=./box_config.json
# BOX_ENTERPRISE_ID=123456

# Mapping DXR label types/subtypes to Box classification keys
DXR_TO_BOX_CLASSIFICATIONS_JSON={
  "default": "Internal",
  "pii": "Confidential",
  "phi": "Highly Confidential"
}

# Ops
RUN_ONCE=1
POLL_SECONDS=60
HTTP_TIMEOUT_SECONDS=20
# Optional smoke limit for DXR
DXR_TAGS_LIMIT=5
